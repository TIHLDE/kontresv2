import BetaCalendar from '@/app/calendar/[id]/components/beta-calendar';

const Page = () => {
    return (
        <div className="max-w-page mx-auto min-h-screen">
            <BetaCalendar className="w-full h-full" />
        </div>
    );
};

export default Page;
